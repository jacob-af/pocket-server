type Profile {
  id: ID!
  image: String
  user: User
  prefferedBook: RecipeBook
  preferredInventory: Inventory
}

input ProfileInput {
  userId: String!
  image: String
}

type Query {
  getProfile(userId: String): Profile!
}

type Mutation {
  updateProfile(image: String): Profile!
}
